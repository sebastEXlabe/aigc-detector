# Security Policy (安全政策)

## 受支持的版本

| 版本 | 支持状态 |
|---|---|
| main（开发版） | ✅ 积极维护 |
| 其他 | ❌ 不再维护 |

## 安全漏洞报告

本项目处理**隐私敏感数据**（学术论文、AIGC 检测报告），安全非常重要。

**请不要**通过公开 Issue 报告安全漏洞（会泄露细节）。

请通过 GitHub 私有漏洞报告功能（仓库 Settings → Security → Report a vulnerability），或直接联系维护者，附上：
- 漏洞类型（如：隐私数据泄露、路径遍历、注入）
- 影响的范围（哪些功能受影响）
- 复现步骤
- 可能的修复建议

## 隐私红线（重要）

- `reports/`（AIGC 检测报告，含论文篇名/作者/单位等个人信息）**严禁公开**。
- 任何含真实个人信息的文件都不得提交进仓库。
- API key、账号密码等凭据必须在 `.gitignore` 中排除（`.credentials.yaml`、`*.key`、`.env`）。

## 建议的安全实践

- 服务默认监听 `127.0.0.1`（仅本机），**勿**改为 `0.0.0.0` 暴露公网。
- 检测接口不鉴权（个人本地工具），如部署到多用户环境须自行加认证。
